---
- hosts: awx
  connection: local
  tasks:
  - name: Create PPTX with Title page
    pptx_file: 
      filename: /tmp/scott.pptx
      title: "Ansible Club"
      subtitle: "This is what you want to do"
